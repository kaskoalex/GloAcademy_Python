print("Введите натуральное число:")
a = int(input())
b = sum(map(int, str(a % 1000)))
print("У числа ", a, " сумма последних трех цифр равна ", b)

Единственно, что я тут подсмотрел, так это фуnкцию map, насколько я понял
именно она заставляет работать sum  с каждым числом в строке.
