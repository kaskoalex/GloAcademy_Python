print("Введите строку натуральных чисел")
number = input()
block = number.split( )
flag = 0
for k in range(len(block)):
    for i in range(1 + k, len(block)):
        if block[k] == block[i]:
            flag += 1
print(flag)
