for h in range(3):
    for m in range(60):
        for s in range(60):
            print(h, ':', m, ':', s)
            
            


print("Введите натуральное число:")
a = int(input())
h = a // 3600
m = a % 3600 // 60
s = a % 3600 % 60
print(h,':',str(m//10)+str(m%10),':',str(s//10)+str(s%10))
