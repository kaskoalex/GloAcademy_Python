def star_date(date):
        block = date.split(".")
        block = [int(i) for i in block]
        a = block[2] % 100
        b = block[0] * block[1]
        if a == b:
                return "True"
        else:
                return "False"
date = input()
print(star_date(date))
